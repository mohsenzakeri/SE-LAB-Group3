<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :players_num ,'تعداد بازیکنان'%><br>
    <%= f.number_field :players_num %>
  </div>
  <div class="field">
    <%= f.label :rounds_num , 'تعداد نوبت‌های بازی'%><br>
    <%= f.number_field :rounds_num %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
